import blindspot from './blindspot';
import echochase from './echochase';
import falselead from './falselead';
import impulse from './impulse';
import lure from './lure';
import snapbacks from './snapbacks';
import swarm from './swarm';
import synapse from './synapse';

const engines = {
  blindspot,
  echochase,
  falselead,
  impulse,
  lure,
  snapbacks,
  swarm,
  synapse,
};

export default engines;
